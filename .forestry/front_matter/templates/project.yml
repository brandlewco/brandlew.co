---
label: project
hide_body: false
fields:
- type: text
  name: title
  label: title
- name: hero_image
  type: file
  config:
    maxSize: 64
  label: Hero Image
  description: Hero image, if applicable
- name: gallery
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: simple
  label: Gallery
